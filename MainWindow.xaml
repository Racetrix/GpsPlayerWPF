<Window x:Class="GpsPlayerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GPS 时间戳对齐工具" Height="750" Width="1100"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.5" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DataLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Foreground" Value="#4EC9B0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="TitleLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="10,5,0,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="📁 当前文件：" VerticalAlignment="Center" Foreground="#AAAAAA"/>
                    <TextBlock x:Name="TxtFilePath" Text="未加载" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0"/>
                </StackPanel>

                <Button x:Name="BtnTopmost" Content="📌 置顶窗口" Click="BtnTopmost_Click" 
                        HorizontalAlignment="Right" Width="100" Background="#444" ToolTip="点击保持窗口在最上层"/>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource CardStyle}" Height="100">
                    <Grid>
                        <TextBlock Text="UTC 时间" Style="{StaticResource TitleLabelStyle}"/>
                        <TextBlock x:Name="LblTime" Text="--:--:--" FontSize="40" Style="{StaticResource DataLabelStyle}" Foreground="White"/>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <TextBlock Text="速度 (km/h)" Style="{StaticResource TitleLabelStyle}"/>
                        <TextBlock x:Name="LblSpeed" Text="0.0" Style="{StaticResource DataLabelStyle}" Foreground="#FF6B6B"/>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}">
                    <Grid>
                        <TextBlock Text="卫星数" Style="{StaticResource TitleLabelStyle}"/>
                        <TextBlock x:Name="LblSats" Text="0" Style="{StaticResource DataLabelStyle}"/>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <TextBlock Text="海拔 (m)" Style="{StaticResource TitleLabelStyle}"/>
                        <TextBlock x:Name="LblAlt" Text="0.0" Style="{StaticResource DataLabelStyle}"/>
                    </Grid>
                </Border>
                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <TextBlock Text="坐标 (Lat / Lon)" Style="{StaticResource TitleLabelStyle}"/>
                        <TextBlock x:Name="LblPos" Text="-- / --" Style="{StaticResource DataLabelStyle}" FontSize="22"/>
                    </Grid>
                </Border>
                <Border Grid.Row="2" Grid.Column="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <TextBlock Text="航向 (Heading)" Style="{StaticResource TitleLabelStyle}"/>
                        <TextBlock x:Name="LblHeading" Text="0°" Style="{StaticResource DataLabelStyle}"/>
                    </Grid>
                </Border>
            </Grid>

            <StackPanel Grid.Row="2" Margin="0,20,0,0">
                <TextBlock x:Name="LblProgress" Text="0 / 0" HorizontalAlignment="Right" Foreground="#888" Margin="0,0,10,5"/>
                <Slider x:Name="TimeSlider" Maximum="100" Value="0" 
                        IsMoveToPointEnabled="True" 
                        PreviewMouseDown="TimeSlider_PreviewMouseDown"
                        PreviewMouseUp="TimeSlider_PreviewMouseUp"
                        ValueChanged="TimeSlider_ValueChanged"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="📂 加载 CSV" Click="BtnLoad_Click"/>
                    <Border Background="#2D2D30" CornerRadius="8" Padding="10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                            <TextBlock Text="打点编辑：" VerticalAlignment="Center" Foreground="#AAAAAA" Margin="0,0,10,0"/>

                            <Button x:Name="BtnMarkIn" Click="BtnMarkIn_Click" 
                Content="🚩 设为起点 (A)" ToolTip="Mark In" Width="100"/>

                            <Button x:Name="BtnMarkOut" Click="BtnMarkOut_Click" 
                Content="🏁 设为终点 (B)" ToolTip="Mark Out" Width="100"/>

                            <Border Background="#444" CornerRadius="4" Padding="10,2" Margin="10,0">
                                <TextBlock x:Name="LblSelectionRange" Text="未选择范围" 
                       Foreground="#FFD700" FontWeight="Bold" VerticalAlignment="Center" Width="160" TextAlignment="Center"/>
                            </Border>

                            <Button Content="💾 保存选中段" Click="BtnSaveRange_Click" Background="#2E7D32" FontWeight="Bold"/>
                            <Button Content="❌ 清除" Click="BtnClearMarks_Click" Width="50"/>

                        </StackPanel>
                    </Border>
                    <Border Width="20"/>
                    <Button Content="⏮" Click="BtnPrev_Click" Width="50"/>
                    <Button x:Name="BtnPlay" Content="▶ 播放" Click="BtnPlay_Click" Width="100" Background="#4CAF50" FontWeight="Bold"/>
                    <Button Content="⏭" Click="BtnNext_Click" Width="50"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid x:Name="ToastOverlay" Opacity="0" IsHitTestVisible="False" VerticalAlignment="Bottom" Margin="0,0,0,100">
            <Border Background="#333333" CornerRadius="25" HorizontalAlignment="Center" Padding="20,10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✅" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ToastText" Text="加载成功" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>